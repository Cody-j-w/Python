from django.shortcuts import render, redirect, HttpResponse


def register(request):
    response = 'placeholder for user registration'
    return HttpResponse(response)

def login(request):
    response = 'placeholder for login screen'
    return HttpResponse(response)

def users(request):
    response = 'placeholder for displaying list of users'
    return HttpResponse(response)